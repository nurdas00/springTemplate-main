package task.controller;import lombok.RequiredArgsConstructor;import lombok.extern.slf4j.Slf4j;import org.springframework.security.core.Authentication;import org.springframework.stereotype.Controller;import org.springframework.ui.Model;import org.springframework.web.bind.annotation.GetMapping;import task.entity.User;@Slf4j@Controller@RequiredArgsConstructorpublic class MainController {    @GetMapping("/index")    public String index() {        return "/index";    }    @GetMapping("/send")    public String send(Model model, Authentication authentication) {        setModel(model, authentication);        return "/sendForm";    }    @GetMapping("/receive")    public String receive(Model model, Authentication authentication) {        setModel(model, authentication);        return "/receiveForm";    }    private void setModel(Model model, Authentication authentication) {//        User user = (User) authentication.getPrincipal();        User user = new User();        user.setName("marat");        user.setLastname("basharov");        model.addAttribute("name", user.getName());        model.addAttribute("lastname", user.getLastname());    }}